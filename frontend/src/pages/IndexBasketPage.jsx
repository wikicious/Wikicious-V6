import AppLayout from '../components/layout/AppLayout';
export default function IndexBasketPage() {
  return <AppLayout><div style={{maxWidth:900,margin:'0 auto',padding:'40px 20px',color:'#EDF0FA'}}>
    <h1 style={{fontSize:28,fontWeight:900,marginBottom:8}}>IndexBasket</h1>
    <p style={{color:'#8892B0',fontSize:14}}>Contract deployed — full UI in progress.</p>
  </div></AppLayout>;
}
